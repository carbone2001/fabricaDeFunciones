/** \brief Ordena vector de estructuras por variables de tipo string
 * \param  El vector de estructuras a ordenar
 * \param El tamanio del vector
 */
void ordAsStructStr(persona pers[],int tam);
/** \brief Ordena vectores de forma ascendente
 * \param El vector a ordenar
 * \param El tamanio del vector ingresado
 */
void ordAs(int * nombreVector, int sizeVector);
void ordAsStructStr(persona pers[],int tam);
/** \brief Ordena vectores de forma descendente
 * \param El vector a ordenar
 * \param El tamanio del vector ingresado
 */
void ordDes(int * nombreVector, int sizeVector);
void ordAsStructStr(persona pers[],int tam);
/** \brief Ordena vectores de forma ascendente mediante el algoritmo de incersion
 * \param El vector a ordenar
 * \param El tamanio del vector ingresado
 */
void ordIncer(int vector[],int lenght);
